services:
  - type: web
    name: predictive-demand-analysis  
    env: python
    plan: free
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "python backend/main.py"  # Adjust if main.py is in a subdirectory
